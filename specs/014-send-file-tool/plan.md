# Implementation Plan: Send File from Ubuntu Container

**Branch**: `main` | **Date**: 2026-06-25 | **Spec**: [spec.md](./spec.md)

**Input**: Feature specification from `specs/014-send-file-tool/spec.md`

## Summary

Add a `send_file` LangChain tool that extracts files from the Ubuntu Docker container `/work/` directory and sends them to QQ group chat as group file attachments via OneBot V11 `MessageSegment.file()`. The tool validates paths to prevent directory traversal, enforces a 10 MB size limit, and only sends files when explicitly requested by the user.

## Technical Context

**Language/Version**: Python 3.12+

**Primary Dependencies**: NoneBot2 (OneBot V11 adapter), LangChain, LangGraph

**Storage**: No persistent storage; temporary files auto-cleaned via `tempfile.TemporaryDirectory()`

**Testing**: pytest with unittest.mock (all docker subprocess calls mocked)

**Target Platform**: macOS/Linux host running NoneBot2 + Docker

**Project Type**: NoneBot2 plugin (QQ chat bot)

**Performance Goals**: File extraction and sending within 30 seconds for files under 10 MB

**Constraints**: 10 MB max file size, `/work/` path whitelist, 30s docker cp timeout

**Scale/Scope**: Single tool addition, ~150 lines of new code, 18 unit tests

## Constitution Check

- ✅ Follows existing @tool patterns in `graph/tools.py`
- ✅ New module `file_transfer.py` has clear single responsibility
- ✅ Config constants in `config.py` under appropriate section
- ✅ Tests mock external dependencies
- ✅ Prompt rules integrated into `role_sys_prompt`
- ✅ Tool registered in both imports and `create_agent()` tools list in `ai.py`

No violations. All gates pass.

## Project Structure

### Documentation (this feature)

```text
specs/014-send-file-tool/
├── plan.md              # This file
├── spec.md              # Feature specification
├── checklists/
│   └── requirements.md  # Spec quality checklist
└── tasks.md             # Task breakdown (generated by /speckit-tasks)
```

### Source Code (repository root)

```text
hatsume/plugins/hatsume-plugin/
├── file_transfer.py     # NEW: Core transfer logic
├── config.py            # MODIFIED: SEND_FILE_* constants
├── prompts.py           # MODIFIED: send_file rules
└── graph/
    ├── tools.py         # MODIFIED: send_file @tool
    └── nodes/
        └── ai.py        # MODIFIED: import + register

tests/
└── test_file_transfer.py # NEW: 18 unit tests
```

## Architecture

```
LLM (chat_agent)
  │ calls send_file(file_path, display_name)
  ▼
graph/tools.py: @tool send_file (thin wrapper)
  │ from ..file_transfer import send_file_to_chat
  ▼
file_transfer.py: send_file_to_chat()
  ├─ validate_container_path() → /work/ restriction + traversal blocking
  ├─ is_container_running()    → docker inspect check
  ├─ is_container_path_a_file() → test -f type check
  ├─ get_container_file_size()  → stat -c%s, rejects >10MB
  ├─ _copy_from_container()    → docker cp to temp dir
  └─ MessageSegment.file()     → send via _ai_answer callback
  └─ shutil.rmtree()           → finally: auto-cleanup
```

## Implementation Status

All code implemented and tested:

| File | Status | Lines |
|------|--------|-------|
| `config.py` | ✅ Modified | +7 (5 constants) |
| `file_transfer.py` | ✅ Created | ~150 |
| `graph/tools.py` | ✅ Modified | +30 (send_file tool) |
| `graph/nodes/ai.py` | ✅ Modified | +2 (import + register) |
| `prompts.py` | ✅ Modified | +8 (tool rules) |
| `test_file_transfer.py` | ✅ Created | ~200 (18 tests) |

Tests: **18/18 PASS** (path validation ×8, size checks ×4, file type ×2, integration ×4)

Commit: `458f58f`
